import s from './Header.module.css';

export default function Header() {
  return (
    <header className={s.Header}>
      <p className={s.headerText}>Test assignment from Klika Tech</p>
    </header>
  );
}
